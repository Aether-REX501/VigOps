# VigOps Backend — Environment Variables
# Copy this file to .env and fill in your token

# GitHub Personal Access Token (repo scope or public_repo)
# Without this, GitHub API allows only 60 requests/hour
# Get one at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Server port (default: 4000)
PORT=4000
